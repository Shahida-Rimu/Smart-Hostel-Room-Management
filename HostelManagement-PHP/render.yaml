services:
  - type: web
    name: hostel-management
    env: php
    buildCommand: |
      apt-get update
      apt-get install -y php php-mysql
      npm install
    startCommand: php -S 0.0.0.0:$PORT
    envVars:
      - key: PHP_VERSION
        value: 8.1
